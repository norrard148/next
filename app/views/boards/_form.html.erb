<%= form_for(board) do |f| %>
  <% if board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
      <% board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field ss">
    <%= f.label :title, :style=> "position: static;font-size:150%;"%>
    <%= f.text_area :title %>
  </div>

  <div class="field ">
    <%= f.label :content, :style=> "position: static;font-size:150%;" %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit :class=>"btn btn-outline-secondary"%>
  </div>
<% end %>
